# Install Skill

## English

```text
Please install cjke84/obsidian-knowledge-organizer as a usable skill. Keep SKILL.md, scripts/, references/, and tests/, keep the skill name as obsidian-knowledge-organizer, confirm it works in OpenClaw and Codex, run pytest -q and verify it passes, and follow the repository instructions for any paths or environment variables. The skill supports destination=obsidian|feishu|ima and mode=once|sync, with Feishu via the official openclaw-lark plugin and IMA via import_doc OpenAPI.
```

## 中文

```text
请把 cjke84/obsidian-knowledge-organizer 安装成可用 skill。保留 SKILL.md、scripts/、references/、tests/，保持 skill 名称为 obsidian-knowledge-organizer，确认它可在 OpenClaw 和 Codex 中使用，安装后运行 pytest -q 并确认通过，路径或环境变量优先遵循仓库说明。这个 skill 支持 destination=obsidian|feishu|ima 和 mode=once|sync，飞书通过 openclaw-lark 插件接入，IMA 通过 import_doc OpenAPI 接入。
```
